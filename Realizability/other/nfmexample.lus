node imported top (
  x : int;
  y : int
) returns (
  z : bool
);
(*@contract
  var guarantee1 : bool = ((x) >= (y)) => (z);
  var guarantee2 : bool = ((x) <= (y)) => (not (z));
  assume (x) <> (y);
  guarantee guarantee1;
  guarantee guarantee2;
*)


